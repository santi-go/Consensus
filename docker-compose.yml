version: '2'
services:
  consensus:
    image: nginx
    ports:
      - '8080:80'
    volumes:
      - '.:/usr/share/nginx/html'
      - './conf/nginx.conf:/etc/nginx/nginx.conf'

  selenium:
    image: selenium/standalone-chrome
    environment:
      - JAVA_OPTS=-Dselenium.LOGGER.level=WARNING
    ports:
      - '4444:4444'

  node:
    image: node:7.9.0
    working_dir: '/test'
    volumes:
      - '.:/test'
    depends_on:
      - consensus
      - selenium
    command: sh -c 'npm install --unsafe-perm && npm run build-watch -s'

volumes:
  bundle:
    driver: local
